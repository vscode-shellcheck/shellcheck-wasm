{
  "name": "@vscode-shellcheck/shellcheck-wasm",
  "version": "0.1.1",
  "description": "ShellCheck compiled to WebAssembly (WASI command module) with a Worker-based runner",
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vscode-shellcheck/shellcheck-wasm.git"
  },
  "files": [
    "dist",
    "!dist/build-info.json",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./worker": {
      "types": "./dist/worker.d.ts",
      "default": "./dist/worker.js"
    },
    "./shellcheck.wasm": "./dist/shellcheck.wasm",
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "gen:version": "node scripts/gen-version.mjs",
    "gen:build-info": "node scripts/gen-build-info.mjs",
    "build": "npm run gen:version && npm run gen:build-info && tsc -p tsconfig.build.json",
    "build:wasm": "scripts/build-wasm.sh",
    "fetch:native": "scripts/fetch-native-shellcheck.sh",
    "bench": "node scripts/bench.mjs",
    "lint": "oxlint",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "test": "vitest run",
    "prepack": "node -e \"require('node:fs').accessSync('dist/shellcheck.wasm')\""
  },
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "^0.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=22"
  }
}
